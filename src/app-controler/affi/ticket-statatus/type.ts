export type ParamStatusTicketType = {
  orderCode: string;
  siteCode: string;
};

export type TicketStatusType = {
  productName: string;
  siteCode: string;
  ticketNumber: string;
  usageStatus: string;
  productCode: string;
  validDateTo: string;
  validDateFrom: string;
};
